{% extends "base.html.twig" %}

{% block body %}
    {% for knowledge in knowledges %}
        <h3>{{ knowledge.name }}</h3>
        <p>{{ knowledge.chapo }}</p>
        <a href="#">En savoir plus</a>
    {% endfor %}
{% endblock body%}
